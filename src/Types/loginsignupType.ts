export interface signupprops {
    name: string
    email: string
    password: string
    role?: string
    clinic_name?: string
    specialization?: string

}

export interface loginprops {
    email: string
    password: string
}

